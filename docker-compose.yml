version: '3.8'

services:
  telegram_bot:
    build: .
    command: python telegram_bot.py
    env_file: .env
    ports:
      - "9001:9001"

  webhook_receiver:
    build: .
    command: uvicorn webhook_receiver:app --host 0.0.0.0 --port 8080
    env_file: .env
    ports:
      - "8080:8080"
